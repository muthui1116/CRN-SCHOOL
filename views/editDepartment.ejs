<%- include('partials/header') %>
<% title = "Edit Department"; %>
<div class="container py-4">
  <div class="row">
    <div class="col-12 col-md-8 offset-md-2">
      <div class="card">
        <div class="card-header">
          <h3>Edit Department — <%= department.name %></h3>
        </div>
        <div class="card-body">
          <form action="/departments/<%= department.id %>/edit" method="POST">
            <div class="mb-3">
              <label>Department Name</label>
              <input name="name" value="<%= department.name %>" class="form-control" required />
            </div>
            <div class="mb-3">
              <label>Head of Department (HOD)</label>
              <select name="hod" class="form-select">
                <option value="">Select a teacher</option>
                <% (teachers||[]).forEach(t => { %>
                  <option value="<%= t.name %>" <%= department.hod === t.name ? 'selected' : '' %>><%= t.name %></option>
                <% }) %>
              </select>
            </div>
            <button class="btn btn-primary">Save Changes</button>
            <a href="/departments" class="btn btn-secondary ms-2">Cancel</a>
          </form>
        </div>
      </div>
    </div>
  </div>
</div>
<%- include('partials/footer') %>
